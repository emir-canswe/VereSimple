<Window x:Class="VereSimple.Views.AddCustomerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Müşteri Ekle" Height="400" Width="450"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="TxtTitle" Grid.Row="0"
                   Text="Yeni Müşteri" FontSize="20" FontWeight="Bold"
                   Foreground="#2C3E50" Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1">
            <TextBlock Text="Ad Soyad *" FontSize="13" Margin="0,0,0,5"/>
            <TextBox x:Name="TxtFullName" Height="36" FontSize="14"
                     Padding="8,0" BorderBrush="#BDC3C7"/>

            <TextBlock Text="Telefon" FontSize="13" Margin="0,12,0,5"/>
            <TextBox x:Name="TxtPhone" Height="36" FontSize="14"
                     Padding="8,0" BorderBrush="#BDC3C7"/>

            <TextBlock Text="Adres" FontSize="13" Margin="0,12,0,5"/>
            <TextBox x:Name="TxtAddress" Height="36" FontSize="14"
                     Padding="8,0" BorderBrush="#BDC3C7"/>

            <TextBlock Text="Not" FontSize="13" Margin="0,12,0,5"/>
            <TextBox x:Name="TxtNotes" Height="60" FontSize="14"
                     Padding="8,5" BorderBrush="#BDC3C7"
                     TextWrapping="Wrap" AcceptsReturn="True"/>

            <TextBlock x:Name="TxtError" Foreground="Red"
                       FontSize="12" Margin="0,8,0,0"
                       Visibility="Collapsed"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="İptal" Width="90" Height="36"
                    Background="#95A5A6" Foreground="White"
                    BorderThickness="0" Cursor="Hand" Margin="0,0,10,0"
                    Click="BtnCancel_Click"/>
            <Button x:Name="BtnSave" Content="Kaydet" Width="90" Height="36"
                    Background="#27AE60" Foreground="White"
                    BorderThickness="0" Cursor="Hand"
                    Click="BtnSave_Click"/>
        </StackPanel>
    </Grid>
</Window>